routes:
# =========================
# Health Check Microservice
# =========================

  - regex: '^/health(/*[a-zA-Z0-9]*)?'
    model: main.daos.Healthcheck
    controller: kotlinserverless.main.controllers.HealthcheckController
    
# =========================
# User Microservice
# =========================

  - regex: '^/user(/*[a-zA-Z0-9]*)?'
    model: main.daos.User
    controller: main.controllers.UserController

# =========================
# Challenges Microservice
# =========================

  - regex: '^/challenge(/*[a-zA-Z0-9]*)?'
    model: main.challenges.models.Challenge
    controller: main.challenges.controllers.ChallengeController

  - regex: '^/challenges(/*[a-zA-Z0-9]*)?'
    model: main.challenges.models.Challenge
    controller: main.challenges.controllers.ChallengesController

# =========================
# Completion Criteria Microservice
# =========================

  - regex: '^/completion(/*[a-zA-Z0-9]*)?'
    model: main.completionCriterias.models.CompletionCriteria
    controller: main.completionCriterias.controllers.CompletionCriteriaController

# =========================
# Rewards Microservice
# =========================

  - regex: '^/reward(/*[a-zA-Z0-9]*)?'
    model: main.rewards.models.Reward
    controller: main.rewards.controllers.RewardController

# =========================
# Token Microservice
# =========================

  - regex: '^/token(/*[a-zA-Z0-9]*)?'
    model: main.tokens.models.Token
    controller: main.tokens.controllers.TokenController

  - regex: '^/tokens(/*[a-zA-Z0-9]*)?'
    model: main.tokens.models.Token
    controller: main.tokens.controllers.TokensController

# =========================
# Transactions Microservice
# =========================

  - regex: '^/transaction(/*[a-zA-Z0-9]*)?'
    model: main.transactions.models.Transaction
    controller: main.transactions.controllers.TransactionController

# =========================
# User Account Microservice
# =========================

  - regex: '^/user_account(/*[a-zA-Z0-9]*)?'
    model: main.userAccounts.models.UserAccount
    controller: main.userAccounts.models.UserAccountController

